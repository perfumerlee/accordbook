# Accordbook versioning

Public versions use `v1.01`, `v1.02`, `v1.03`, and so on.

Package versions map to SemVer patch releases: `v1.01` → `1.0.1`, `v1.02` → `1.0.2`.

Existing releases and tags are not retroactively changed.
