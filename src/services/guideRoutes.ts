export type GuideRoute = { kind: 'GUIDE_HOME' } | { kind: 'GUIDE_LOCALE_HOME'; locale: 'en' | 'ko' } | { kind: 'GUIDE_CHAPTER'; locale: 'en' | 'ko'; slug: string } | { kind: 'GUIDE_NOT_FOUND' } | { kind: 'NOT_GUIDE_ROUTE' }
export function resolveGuideRoute(pathname: string): GuideRoute { if (pathname === '/guide' || pathname === '/guide/') return { kind: 'GUIDE_HOME' }; const parts = pathname.replace(/\/$/, '').split('/'); if (parts[1] !== 'guide') return { kind: 'NOT_GUIDE_ROUTE' }; if (parts.length === 3 && (parts[2] === 'en' || parts[2] === 'ko')) return { kind: 'GUIDE_LOCALE_HOME', locale: parts[2] }; if (parts.length === 4 && (parts[2] === 'en' || parts[2] === 'ko') && id.test(parts[3])) return { kind: 'GUIDE_CHAPTER', locale: parts[2], slug: parts[3] }; return { kind: 'GUIDE_NOT_FOUND' } }
const id = /^[a-z0-9]+(?:-[a-z0-9]+)*$/
